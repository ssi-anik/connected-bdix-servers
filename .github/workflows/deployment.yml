name: Deploy to server
on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v1
        with:
          node-version: '14'

      - name: Install yarn
        run: npm install -g yarn

      - name: Install Packages
        run: yarn install

      - name: Build project
        run: yarn run build

      - name: Deploy files to byethost
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.FTP_SERVER}}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./build/
          server-dir: ${{ secrets.FTP_UPLOAD_DIR }}

      - name: Deploy files to helio.us
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.FTP_HELIO_SERVER}}
          username: ${{ secrets.FTP_HELIO_USERNAME }}
          password: ${{ secrets.FTP_HELIO_PASSWORD }}
          local-dir: ./build/
          server-dir: ${{ secrets.FTP_HELIO_UPLOAD_DIR }}

      - name: Deploy files to freehosting.io
        uses: SamKirkland/FTP-Deploy-Action@4.0.0
        with:
          server: ${{ secrets.FTP_FREEHOSTING_IO_SERVER}}
          username: ${{ secrets.FTP_FREEHOSTING_IO_USERNAME }}
          password: ${{ secrets.FTP_FREEHOSTING_IO_PASSWORD }}
          local-dir: ./build/
          server-dir: ${{ secrets.FTP_FREEHOSTING_IO_UPLOAD_DIR }}

